import { PortalPlaceholder } from "@/components/layout/portal-placeholder";

export const metadata = {
  title: "Profile",
};

export default function Page() {
  return (
    <PortalPlaceholder
      title="Profile"
      description="Manage your credit manager profile."
    />
  );
}

